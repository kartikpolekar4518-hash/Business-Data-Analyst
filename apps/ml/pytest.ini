[pytest]
# shap and matplotlib announce their own future deprecations on import; that is
# noise here. A warning raised by this service's own code is not filtered.
filterwarnings =
    ignore::PendingDeprecationWarning
