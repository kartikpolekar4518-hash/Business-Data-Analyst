# --- Vercel / Frontend Environment Variables ---
# Production API URL (e.g. https://api.yourdomain.com)
# Leave empty for local development (Vite proxy handles /api → localhost:4000)
VITE_API_URL=

# Brand name displayed in the UI header and landing page
VITE_APP_NAME=NoPS
